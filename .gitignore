# ---- Node -----------------------------------------------------------------
node_modules/
jspm_packages/
.npm
.node_repl_history
.yarn-integrity
*.tgz

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Test / coverage output
coverage/
*.lcov
.nyc_output/

# Caches
.eslintcache
.cache/
.parcel-cache/

# Build output (unused today -- there is no build step)
dist/
build/

# ---- Secrets and runtime state ---------------------------------------------
# .env holds the service account key, the Smartsheet secret and the Discord
# webhook. The .example is the committed template and must stay tracked.
.env
.env.local
.env.*.local
!.env.example

# OAuth tokens (tokens.json) and the watermark (state.json), mounted into the
# container as /data.
data/

# ---- Editors and OS ---------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db
desktop.ini
